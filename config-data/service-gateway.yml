server:
  port: 8099
spring:
  application:
    name: service-gateway
  cloud:
    gateway:
      routes:
      - id: service-product
        uri: lb://service-product
        predicates:
        - Path=/product/**

managment:
  edpoints:
    web:
      exposure:
        include: "*"
      